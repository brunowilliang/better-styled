{
  "name": "use-styled-monorepo",
  "private": true,
  "packageManager": "bun@1.3.1",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "biome check ./packages/lib-v2",
    "format": "biome format ./packages/lib-v2 --write"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.13",
    "turbo": "^2.5.0",
    "typescript": "^5.0.0"
  },
  "overrides": {
    "react": "19.1.0",
    "react-dom": "19.1.0"
  }
}